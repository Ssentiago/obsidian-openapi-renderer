import { Plugin } from 'obsidian';

export class OpenAPIRendererPlugin extends Plugin {
    onload(): Promise<void> | void {
        super.onload();
    }
    onunload(): Promise<void> | void {
        super.onunload();
    }
}
