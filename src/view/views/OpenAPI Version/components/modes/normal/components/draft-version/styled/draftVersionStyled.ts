import styled from 'styled-components';

export const RestoredLabel = styled.div`
    background-color: var(--color-base-25);
    color: var(--color-blue);
    padding: 8px 16px;
    border-radius: 8px;
    font-size: 14px;
    font-weight: bold;
    display: inline-block;
    box-shadow: 0px 2px 4px rgba(var(--mono-rgb-100), 0.1);
    margin-top: 16px;
`;
